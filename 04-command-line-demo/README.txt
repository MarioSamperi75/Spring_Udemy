Starting from console:

		cd				--> (to the right folder)

Windows: 
(Linux & Mac ./mvnw)

	    mvnw package	--> (update and create the jar file in target folder)
		cd target
		dir 			--> (to read -and copy- the jar file name)
		java -jar jarfilename.jar
		 
 alternative with spring-boot maven plugin: 
 		mvnw spring-boot:run
 