# ACTUATOR

info.app.name=My Super Cool App
info.app.description=A crazy fun app, yoohoo!
info.app.version=1.0.0
# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list

# Base path for actuator Endpoints
management.endpoints.web.base-path=/newbasepathnolongeractuator 

management.endpoints.web.exposure.include=*
# Exclude individual endpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=health,info


# CUSTOM PROPERTIES
coach.name=Trap
team.name=Juventus



# WEB PROPERTIES
# Change Sprng Boot embedded server port
server.port=7070

# Set the context path of the application
# All the requests should prefixed with /mycoolapp

server.servlet.context-path=/mycoolapp

# Default HTTP session timeout (default 30m)
# server.servlet.session.timeout=15m

#SECURITY PROPERTIES
# spring.security.user.name=Mario
# spring.security.user.password=NonTeLaDico

#DATA PROPERTIES

#JDBC URL of the database
spring.datasource.url=jdbc:	mysql://localhost:7070/somedatabase
spring.datasource.username=Mario
spring.datasource.password=NonTeLaDico
